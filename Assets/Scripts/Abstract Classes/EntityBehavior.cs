using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EntityBehavior : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void EntityDamage(int dmg)
    {
       // CombatManager.Instance.entityClass.TakeDamage(dmg);
    }

    public void EntityHeal(int val)
    {
       // CombatManager.Instance.entityClass.Heal(val);
    }
}
